const color = {
};

const fontFamily = {
  primary: '\'PingFangTC\', \'微軟正黑體\', \'Microsoft JhengHei\', sans-serif',
};

const font = {
  family: fontFamily,
};

const theme = {
  font,
  color,
};

export default theme;
